<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Original used @style/AppTheme.Launcher / NormalTheme / CameraScanTheme.
         No AppCompat/Material dependency in the rebuild, so plain platform themes. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="AppTheme" parent="@android:style/Theme.Material.Light.NoActionBar" />
    <style name="AppTheme.Launcher" parent="AppTheme">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Material.Light.NoActionBar" />
    <style name="CameraScanTheme" parent="@android:style/Theme.Material.NoActionBar.Fullscreen" />
</resources>
