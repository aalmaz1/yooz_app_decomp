import 'package:flutter/material.dart';

class ThemeListPage extends StatelessWidget {
  const ThemeListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Theme library')),
    );
  }
}
